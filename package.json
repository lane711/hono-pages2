{
  "name": "hono-pages2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "run-p dev:*",
    "dev:wrangler": "wrangler pages dev dist --live-reload",
    "dev:esbuild": "rsync -avzh ./public ./dist --delete && esbuild --bundle src/server.js --format=esm --watch --outfile=dist/_worker.js",
    "build": " rsync -avzh ./public ./dist --delete && esbuild --bundle src/server.js --format=esm --outfile=dist/_worker.js",
    "deploy": "wrangler pages publish dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.18",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.0.4",
    "wrangler": "^2.20.0"
  },
  "dependencies": {
    "hono": "^3.1.8"
  }
}
